# EGX Data Engine - Requirements

# Core for server.py - uses built-in modules only
# No external dependencies required for basic HTTP server

# Data Processing (for data_engine.py and analysis services)
numpy>=1.24.0
pandas>=2.0.0
loguru>=0.7.0
requests>=2.31.0
